# Model Directory

This directory is a placeholder for the TFLite model local fallback.
The model is primarily downloaded from Firebase ML at runtime.
For local development/testing, place `1.tflite` here and rename to `model.tflite`,
or keep the directory empty if using only Firebase ML download.
